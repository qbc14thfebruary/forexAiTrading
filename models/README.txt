Đặt 2 file model đã train vào đây:
  - clf_model.json  (model phân loại hướng đi)
  - reg_model.json  (model hồi quy biên độ)

Lấy 2 file này từ Google Colab sau khi chạy xong notebook/train_walk_forward.ipynb
(tại <data_folder>/models/ trên Google Drive), tải về và copy đè vào thư mục này.
